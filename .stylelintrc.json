{
  "extends": ["stylelint-config-tailwindcss"],
  "ignoreFiles": ["node_modules/**", ".next/**", "dist/**"],
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["tailwind", "apply", "layer", "respond-to"]
      }
    ]
  }
}
